<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Liste des utilisateurs</h1>
            <a href="/admin/user/new">Ajouter un nouvel utilisateur</a>
            <table class="table">
                <thead>
                <tr>
                    <th>Nom d'utilisateur</th>
                    <th>Prénom</th>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Job</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for user in users %}
                    <tr>
                        <td>{{ user.username }}</td>
                        <td>{{ user.firstname }}</td>
                        <td>{{ user.lastname }}</td>
                        <td>{{ user.email }}</td>
                        <td>{{ user.jobtitle }}</td>
                        <td colspan="3">
                            <a href="/admin/user/{{ user.id }}/details">Voir</a>
                            <a href="/admin/user/{{ user.id }}/edit">Modifier</a>
                            <a href="/admin/user/{{ user.id }}/delete">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
</body>
</html>